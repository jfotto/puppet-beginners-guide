package { 'cowsay':
  ensure => installed,
}
package { 'httpd':
  ensure => installed,
}
package { 'htop':
  ensure => installed,
}
package { 'vim':
  ensure => installed,
}
package { 'sysdig':
  ensure => installed,
}
